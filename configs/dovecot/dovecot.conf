# ====================================================================
# CPANEL1280 DOVECOT MASTER CONFIGURATION (dovecot.conf)
# ====================================================================

!include_try /usr/share/dovecot/protocols.d/*.protocol
protocols = imap pop3 lmtp

listen = *, ::

!include conf.d/*.conf
!include_try local.conf
